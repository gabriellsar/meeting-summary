<section class="hero">
    <h1>
        Reuniões mais inteligentes, <br />
        <span class="text-gradient">Resumos automáticos</span>
    </h1>
    <p class="subtitle">
        Transforme gravações de áudio em atas estruturadas, decisões e listas de tarefas em segundos, com o poder da Inteligência Artificial.
    </p>
</section>

<style>
    .hero {
    text-align: center;
    padding: 4rem 1rem 3rem;
    max-width: 900px;
    margin: 0 auto;

}

.hero h1 {
    font-family: var(--font-family-headings);
    font-size: 3.5rem;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    font-weight: 800;
    letter-spacing: -0.02em;
    color: var(--color-text-dark);
}

.hero .text-gradient {
    background: linear-gradient(
        to right,
        var(--color-primary-dark), 
        var(--color-secondary-light)
    );
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    display: inline-block;
}

.hero .subtitle {
    font-family: var(--font-family-body); 
    font-size: 1.25rem;
    color: var(--color-text-dark);
    opacity: 0.8;
    line-height: 1.6;
    max-width: 650px;
    margin: 0 auto;
}
</style>